Schampignon is an experimental interpreter, related to the
Virtua programming language: http://manuel.github.com/virtua

The master branch is Scheme-like.

The fexpr branch is Kernel-like.

The fexpr-nt branch is Kernel-like, but with an entirely different
implementation.
